const turnLed = (req, res) => {
  //Nodemcu change led status and response to server if success
};

const readSensor = (req, res) => {
  //Nodemcu read sensor and sen value via response to server if success
};

module.exports = {
  turnLeds,
  readSensor
};
